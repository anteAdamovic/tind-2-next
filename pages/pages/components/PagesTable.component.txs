import React from 'react';
import { Table } from '@tindtechnologies/tind-components';

export interface Page {
    name: string;
}

export interface PagesTableProps {
    pages: Page[]; 
}

export const PagesTable = ({
    pages
}: PagesTableProps) => {
    return (
        <>
            <Table />
        </>
    );
}